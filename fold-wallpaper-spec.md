# FoldWallpaper 仕様書

## 1. 概要

Google Pixel 11 Pro Fold（内側/外側の2枚のディスプレイを持つ折りたたみスマートフォン）向けのライブ壁紙アプリ。
展開／折りたたみの状態を検知し、内側ディスプレイと外側ディスプレイでそれぞれ異なる壁紙画像を表示する。

- 対象端末: **Google Pixel 11 Pro Fold のみ**。実機は同時デュアル表示ではなく、開閉に応じて有効なディスプレイが切り替わる方式。
- ただし、将来的にFoldシリーズの後継機に買い替えた場合でも使い続けられるよう、解像度・アスペクト比などの**機種依存の値はコード中に直接埋め込まず、設定用の定数/変数として一箇所にまとめ、書き換えが容易な形にしておく**（詳細は4章参照）。
- プロジェクト名: FoldWallpaper
- 言語: Kotlin
- Minimum SDK: **API 36**（対象がPixel 11 Pro Fold 1台のみのため、互換分岐を持たない方針で決定。当初案のAPI 30から変更）
- 開発環境: Android Studio（プロジェクト作成済み）／実装はClaude Codeで行う

## 2. 動作の基本原理

- 端末は開閉のたびに「アクティブなDisplay」が切り替わり、OSはライブ壁紙の`WallpaperService.Engine`をその都度破棄・再生成する。
- 各`Engine`は、アタッチされている`Display`の実寸（アスペクト比）から「内側」か「外側」かを判定できる。
  - 外側ディスプレイ: 縦長（幅が狭い）
  - 内側ディスプレイ: ほぼ正方形に近い横長〜正方形
- この判定結果に応じて、表示する画像を切り替える。

## 3. 機能要件

### 3.1 画像の管理方法
- ユーザーが端末のギャラリー（フォトピッカー）から画像を選択する方式。
- アプリ内蔵の固定画像は使用しない。

### 3.2 壁紙の表現
- 静止画のみ。アニメーションやパララックス等の動きは不要。

### 3.3 設定画面 — フェーズ分け

**フェーズ1（初期実装・MVP）**
- 設定画面から画像を1枚選択できるだけのシンプルな構成。
- 内側用・外側用を区別せず、まずは「1枚選んでライブ壁紙として表示できる」ところまでを実装する。
- 画像選択にはAndroid 13以降の Photo Picker（`ActivityResultContracts.PickVisualMedia`）を使用。

**フェーズ2（最終形）**
- 内側画面用の画像フォルダ・外側画面用の画像フォルダを、それぞれ別々に設定できるようにする。
- フォルダ選択には Storage Access Framework（`ACTION_OPEN_DOCUMENT_TREE`）を使用し、`takePersistableUriPermission`で永続的なアクセス権を保持する。
- フォルダ内に複数枚の画像がある場合の選択ロジック（例: ランダム表示／一定時間ごとに切替／常に固定の1枚目、など）は **要検討事項**。Claude Codeでの実装時に選択肢を提示し、方針を決定する。

## 4. アーキテクチャ方針

- `WallpaperService` + `Engine` を中心に実装。
  - `Engine.onCreate(SurfaceHolder)` 内で `displayContext?.display` の実寸からアスペクト比を計算し、内側/外側を判定する。
  - 判定結果に応じて、保存済みの画像URI（内側用/外側用）から該当する画像を読み込み描画する。
- 設定用の `MainActivity`（or `SettingsActivity`）を用意し、画像/フォルダ選択UIを提供する。
- 選択した画像URI・フォルダURIは `SharedPreferences` または `DataStore` に永続化する。
  - フェーズ2ではフォルダURIに対する永続パーミッション（`takePersistableUriPermission`）の管理も必要。
- ライフサイクル上の注意点として、Engineは開閉のたびに破棄・再生成されるため、画像読み込みはできるだけ軽量・高速に行い、切替時のちらつきを最小限にする設計とする。

### 4.1 機種依存値の切り出し方針

- Pixel 11 Pro Foldの内側/外側ディスプレイのアスペクト比・解像度など、**開閉判定に使う機種固有の数値は、専用のオブジェクト（例: `DeviceProfile.kt`のようなファイル）に定数としてまとめる**。
  - 例: 内側/外側それぞれの想定アスペクト比の範囲、判定用しきい値など。
  - 判定ロジック本体（`Engine`側）は、この定数を参照するだけにし、値そのものをロジックの中に直接書かない。
- 将来的にFoldシリーズの後継機へ買い替えた場合は、この定数ファイルの値を書き換えるだけで対応できることを目指す（ロジックの改修は不要な設計とする）。
- 実機での実測値が確定するまでは仮の値を入れておき、後から更新できるようにする。

## 5. 非機能要件

- オフラインで完結すること（外部通信不要）。
- 端末内の画像のみを扱い、個人情報を外部送信しない。
- 開閉の切替時に大きな遅延やクラッシュが起きないこと。

## 6. マニフェスト・設定関連（実装時の参考）

- `WallpaperService` の `<service>` 宣言、`BIND_WALLPAPER` パーミッション。
- ライブ壁紙のメタデータXML（`res/xml/wallpaper.xml`）の用意。
- Photo Picker / SAF に必要なパーミッション設定（スコープドストレージ対応、`READ_MEDIA_IMAGES`は基本的にPhoto Picker利用時は不要）。

## 7. 決定事項・残課題

### 7.1 決定済み（実装済み）

- **minSdk: 36**（1章参照）。
- **フェーズ2のフォルダ内画像選択: 開閉のたびにランダム**（直前と同じ画像は避ける）。ロジックはフェーズ2で実装。
- **アスペクト比が合わない画像の表示: 中央クロップで全面表示**（レターボックスなし）。
- **実装スコープ（今回）: フェーズ1＋内側/外側の2枚出し分けまで**。SAFによるフォルダ選択はフェーズ2に送る。

### 7.2 仕様からの変更点（実装時に判明した制約）

- **Photo Picker のURIは永続化できない。** `PickVisualMedia` が返すURIは `takePersistableUriPermission` の対象外で、権限が再起動で失効する。ライブ壁紙は常駐して再起動後も描画するため、URIを保持する方式では壁紙が消える。
  → **選択時に画像の実体をアプリ内部ストレージ（`filesDir/wallpapers/`）へコピーし、Engineはローカルファイルのみを読む方式**に変更。フェーズ2のSAFフォルダURIは `takePersistableUriPermission` が有効なので、そちらはURI保持でよい。
- **面の判定は `onSurfaceChanged` で行う。** 「開閉のたびにEngineが破棄・再生成される」という前提は機種/OSによっては成り立たず、Engineが生き残ったままサーフェスのサイズだけ変わる場合がある。どちらの経路でも同じ判定が走るよう、`onCreate` ではなく `onSurfaceChanged(width, height)` の実寸で判定する。

### 7.3 残課題

- **`DeviceProfile.kt` のアスペクト比レンジは実機実測前の暫定値。** 実機で内側/外側それぞれ設定画面を開き、「この画面の実測値」カードに出る値でレンジを更新すること。想定レンジ外の場合はカードに警告が出る。
- フェーズ2（内側/外側それぞれのフォルダ選択、SAF、ランダム切替）の実装。
- テンプレート由来の未使用リソース（`purple_*` 等）と、`core-ktx` / `lifecycle-runtime-ktx` の古いバージョンの整理。
