# FoldWallpaper 仕様書

## 1. 概要

Google Pixel 11 Pro Fold（内側/外側の2枚のディスプレイを持つ折りたたみスマートフォン）向けのライブ壁紙アプリ。

2つの軸で壁紙を出し分ける。

- **開閉**（内側ディスプレイ／外側ディスプレイ）
- **プロファイル**（プライベート／標準）— マナーモードで判定する

それぞれの組み合わせに画像フォルダを割り当て、フォルダ内からランダムに1枚を表示する。

| 項目 | 内容 |
|---|---|
| 対象端末 | **Google Pixel 11 Pro Fold**。開閉に応じて有効なディスプレイが切り替わる方式（同時デュアル表示ではない） |
| プロジェクト名 | FoldWallpaper |
| 言語 | Kotlin（Jetpack Compose） |
| Minimum SDK | **API 36**（対象が1機種のみのため互換分岐を持たない方針。当初案のAPI 30から変更） |
| 外部通信 | なし。端末内で完結 |

将来的にFoldシリーズの後継機へ買い替えても使い続けられるよう、解像度・アスペクト比などの**機種依存の値はコード中に直接埋め込まず、`DeviceProfile.kt` に集約**している（4.1参照）。

## 2. 動作の基本原理

### 2.1 面（内側／外側）の判定

`WallpaperService.Engine` の `onSurfaceChanged(width, height)` に渡される**描画領域の実寸**からアスペクト比（長辺/短辺）を求め、内側か外側かを判定する。

- 外側ディスプレイ: 縦長（実測 2.169）
- 内側ディスプレイ: ほぼ正方形（実測 1.037）

判定を `onCreate` ではなく `onSurfaceChanged` で行うのは、**「開閉のたびにEngineが破棄・再生成される」という前提が機種やOSによっては成り立たない**ため。Engineが生き残ったままサーフェスのサイズだけ変わる場合でも、同じ経路で判定できるようにしている。

「前回描画した面」は永続化する。Engineが作り直されてもプロセスが死んでも、開閉を取りこぼさずに検知するため。

### 2.2 プロファイル（プライベート／標準）の判定

マナーモードの状態から判定する。詳細と判定の向きは10章を参照。

### 2.3 画像の切り替え

描画の契機ごとに、次のいずれかを満たせば別の画像に進める。

1. 前回描画した面と違う（＝開閉した）
2. 前回描画したプロファイルと違う（＝マナーモードが切り替わった）
3. 最終切替から設定間隔が経過している
4. 表示中の画像のキャッシュが無い

## 3. 機能要件

### 3.1 画像の管理方法

- ユーザーが Storage Access Framework（`ACTION_OPEN_DOCUMENT_TREE`）で**画像フォルダを指定**する。アプリ内蔵の固定画像は持たない。
- 指定先は「面 × プロファイル」の**4通り**（内側×プライベート、内側×標準、外側×プライベート、外側×標準）。
- フォルダ直下の画像のみを対象とし、サブフォルダには入らない。
- フォルダ内からランダムに1枚選ぶ。直前に表示した1枚は避ける。
- 詳細は8章を参照。

### 3.2 壁紙の表現

- 静止画のみ。アニメーションやパララックス等の動きは持たない。
- アスペクト比が合わない画像は**中央クロップで全面表示**する（レターボックスなし）。
- 表示時にアプリ側で暗くできる。内側／外側で別々に指定し、プロファイルには依存しない（9章）。

### 3.3 プロファイル

- **プライベート**と**標準**の2つ固定。
- マナーモードで判定し、判定できない場合は必ず標準に倒す（10章）。

### 3.4 画面構成

- **設定画面** — 面ごとにプレビュー・暗さスライダー・フォルダ設定（プロファイル別）、切替間隔、ライブ壁紙の適用ボタン。画面幅に応じて2面を横並びとタブで切り替える（9.5）。
- **情報画面** — プライベート判定の状態、壁紙の描画領域の実測値、設定画面のウィンドウサイズ（11.1）。

## 4. アーキテクチャ

`WallpaperService` + `Engine` を中心に実装し、設定は `SharedPreferences` に永続化する。

### 4.1 機種依存値の切り出し方針

開閉判定に使う機種固有の数値は `DeviceProfile.kt` に定数としてまとめ、判定ロジック本体はこの定数を参照するだけにしている。後継機へ買い替えた場合は、この定数ファイルの値を書き換えるだけで対応できることを目指す（ロジックの改修は不要な設計）。

同じ思想で、調整したくなる値はそれぞれ1箇所に集約している。

| ファイル | 集約している値 |
|---|---|
| `DeviceProfile.kt` | 内側／外側のアスペクト比レンジ、判定しきい値 |
| `WallpaperDimming.kt` | 暗さの上限・初期値 |
| `WallpaperProfile.kt` | マナーモードとプロファイルの対応（判定の向き） |
| `SwitchInterval.kt` | 時間経過での切替間隔の選択肢 |

### 4.2 主要ファイル

| ファイル | 役割 |
|---|---|
| `DeviceProfile.kt` | 面の判定。機種依存値の集約先 |
| `WallpaperProfile.kt` | プロファイルの定義と、マナーモードからの判定 |
| `WallpaperDimming.kt` | 暗さの定義とアルファ値への変換 |
| `SwitchInterval.kt` | 時間経過での切替間隔 |
| `data/WallpaperStore.kt` | 設定の永続化と、画像のローカルキャッシュ管理 |
| `data/ImageFolderScanner.kt` | SAFフォルダ直下の画像列挙 |
| `wallpaper/FoldWallpaperService.kt` | ライブ壁紙本体。判定・切替・描画の制御 |
| `wallpaper/ImageRotator.kt` | 画像を進める／先読みする |
| `wallpaper/ImageSelection.kt` | ランダム選択（直前回避） |
| `wallpaper/WallpaperRenderer.kt` | デコード・中央クロップ・暗さの描画 |
| `wallpaper/WallpaperImageCache.kt` | 面ごと1枚のビットマップ保持 |
| `MainActivity.kt` | 設定画面と情報画面 |

### 4.3 パフォーマンス上の設計

- 開閉のたびに描画が走るため、**次に表示する1枚を先読み**して内部ストレージに置く。切替時はファイルを昇格させるだけで済み、SAFの走査もデコードも挟まらない（8.3）。
- 画像のデコードは100ms単位でかかるためメインスレッドでは行えない。一方 `lockCanvas` は直列化が必要なので、Engineごとに**単一スレッドのexecutor**を用意し、SAFアクセス・デコード・描画をすべてそこで行う。
- ファイル一覧は `SharedPreferences` ではなく内部ファイルに置く。`SharedPreferences` は初回アクセス時に全キーを読み込むため、数千件の一覧を混ぜると描画経路が遅くなる。

## 5. 非機能要件

- オフラインで完結すること（外部通信不要）。
- 端末内の画像のみを扱い、個人情報を外部送信しない。
- 開閉の切替時に大きな遅延やクラッシュが起きないこと。
- フォルダが空・権限失効・デコード失敗のいずれでも、**壁紙が黒くならないこと**（8.4）。

## 6. マニフェスト・権限

- `WallpaperService` の `<service>` 宣言と `BIND_WALLPAPER` パーミッション。
- ライブ壁紙のメタデータXML（`res/xml/wallpaper.xml`）。`settingsActivity` に `MainActivity` を指定。
- `<uses-feature android:name="android.software.live_wallpaper" android:required="true" />`。
- **実行時パーミッションは一切不要**。
  - 画像へのアクセスは SAF の永続URIパーミッションで行うため `READ_MEDIA_IMAGES` は不要。
  - マナーモードの読み取り（`AudioManager.getRingerMode()`）と変更通知の受信も権限不要（10章）。

## 7. 設計判断の記録

1〜6章は現在の実装を記述している。この章以降は**そうなっている理由**と、調査して分かった制約を残す。同じ検討を繰り返さないための記録。

### 7.1 当初の未確定事項とその決定

| 論点 | 決定 |
|---|---|
| minSdk | **36**。対象が1機種のみで互換分岐が不要なため（当初案は30） |
| フォルダ内に複数画像があるときの選び方 | **開閉のたびにランダム**。直前と同じ画像は避ける |
| アスペクト比が合わない画像の表示 | **中央クロップで全面表示**（レターボックスなし） |
| 画像の指定方法 | 当初は1枚選択から始め、**フォルダ指定に一本化**した |

### 7.2 当初の想定から変えた設計と、その理由

- **Photo Picker をやめ、SAF のフォルダ指定にした。** `PickVisualMedia` が返すURIは `takePersistableUriPermission` の対象外で、権限がプロセス終了や再起動で失効する。ライブ壁紙は常駐して再起動後も描画するため、URIを保持する方式では壁紙が黒くなる。
  → 画像の実体をアプリ内部ストレージへコピーし、Engineはローカルファイルのみを読む方式にした。SAFのフォルダURIは `takePersistableUriPermission` が有効なので、そちらはURI保持でよい。
- **面の判定を `onCreate` から `onSurfaceChanged` に移した。** 「開閉のたびにEngineが破棄・再生成される」という当初の前提は機種/OSによっては成り立たず、Engineが生き残ったままサーフェスのサイズだけ変わる場合がある。どちらの経路でも同じ判定が走るようにした（2.1）。

### 7.3 実機実測値（Pixel 11 Pro Fold）

実機で動作確認し、以下の実測値を得た。`DeviceProfile.kt` のレンジはこの値に合わせて更新済み。

| | 描画領域 | アスペクト比（長辺/短辺） | 判定 |
|---|---|---|---|
| 内側（開いた状態） | 2076 × 2152 px | 1.037 | INNER |
| 外側（閉じた状態） | 1080 × 2342 px | 2.169 | OUTER |

- 更新後のレンジ: 内側 `1.00f..1.30f` / 外側 `1.90f..2.50f`、しきい値 `1.55f`（据え置き）。
- 上記は**設定画面のウィンドウサイズ**の値。当初は壁紙サーフェスの実寸と数十px単位でずれる可能性を想定していたが、段階2で Engine 側の実測値を記録して比較したところ、**両者は一致した**（実機確認済み）。したがって壁紙素材はこの解像度のまま作ってよい。
- ただしレンジには余裕を残してある。将来の機種でずれが生じた場合に吸収するため、これ以上狭めないこと。

### 7.4 片付け（対応済み）

- テンプレート由来の未使用リソース `res/values/colors.xml`（`purple_*` 等7色）を削除。Compose の配色は `ui/theme/Color.kt` で完結しており、XML の色リソースは参照されていなかった。
- `Theme.kt` のダイナミックカラー判定から `Build.VERSION.SDK_INT >= S` を削除（minSdk 36 では常に真）。
- 依存を最新へ更新：AGP 9.4.1 / Kotlin Compose プラグイン 2.4.20 / Compose BOM 2026.09.00 / core-ktx 1.19.0 / lifecycle-runtime-ktx 2.11.0 / activity-compose 1.13.0 / espresso-core 3.7.0 / androidx.test.ext:junit 1.3.0。
- `.idea/deploymentTargetSelector.xml`（Android Studio がデプロイ先端末を記録するローカル設定）を追跡対象から外し、`.gitignore` に追加。

この時点で `lintDebug` の指摘は0件。

## 8. フォルダ指定と画像の切り替え

### 8.1 決定事項

| 項目 | 決定 |
|---|---|
| 画像が切り替わる契機 | **開閉したとき**（＝判定される面が変わったとき）**／プロファイルが変わったとき／一定時間経過後** |
| 切替間隔 | **設定画面で選択**（なし / 15分 / 1時間 / 6時間） |
| 画像の持ち方 | **次の1枚を先読みして内部ストレージにキャッシュ** |
| サブフォルダ | **指定フォルダ直下の画像のみ**（再帰しない） |
| 選択方法 | ランダム。直前に表示した1枚は避ける |

### 8.2 切替ロジック

面（内側/外側）ごとに独立して、直前に表示した画像と最終切替時刻を保持する。

描画の契機（`onSurfaceChanged` / `onVisibilityChanged(true)` / マナーモードの変更通知）ごとに、次のいずれかを満たせば別の画像に進める。

1. **前回描画した面と今回の面が違う**（＝開閉した）
2. **前回描画したプロファイルと違う**（＝マナーモードが切り替わった。10.4参照）
3. **最終切替時刻から設定間隔が経過している**（間隔が「なし」なら判定しない）
4. **現在の画像のキャッシュが失われている**

「前回描画した面」と「前回描画したプロファイル」はどちらも永続化する。Engineが破棄・再生成されても、プロセスが死んで復帰しても、変化を取りこぼさずに判定するため。

回転などで同じ面・同じプロファイルのまま `onSurfaceChanged` が呼ばれた場合は、条件1・2に当たらないので画像は変わらない。

### 8.3 先読み

描画を終えた後、バックグラウンドで次回用の画像を1枚選び、内部ストレージへコピーしておく。切替時はこのファイルを現在の画像へ昇格させるだけで済み、SAFの一覧取得もデコードも挟まらない。

先読みはメモリと内部ファイルの両方に置く。壁紙プロセスが落ちて再生成されても、再生成直後から即座に描画できる。

内部ストレージの使用量は、面ごとに「現在」＋「次」の最大2枚、合計4枚分にとどまる。

### 8.4 異常時の挙動

フォルダが空、アクセス権が失効、デコード失敗のいずれの場合も、**直前にキャッシュした画像を描き続ける**。壁紙が黒くなる状態を避けるため。キャッシュも無い場合のみ単色で塗る。設定画面側に状態を表示する。

### 8.5 一覧の取得と再スキャン

- 対象は MIME が `image/` で始まるファイルのみ。
- 一覧は `DocumentsContract.buildChildDocumentsUriUsingTree()` に対する**単発クエリ**で取得する。`DocumentFile.listFiles()` は1ファイルごとにクエリを発行するため、数百枚のフォルダで数秒かかる。使用しない。
- 取得した一覧は永続化し、**設定画面を開いたときだけ**再スキャンする。壁紙の描画経路では再スキャンしない。

### 8.6 SAF上の制約（実装・運用時の注意）

- `Download` のルート、`Android/data`、`Android/obb` は Android 11 以降 OS が選択を拒否する。`DCIM` や `Pictures` 配下を使うこと。
- `takePersistableUriPermission` で取得した権限は端末の再起動では保持されるが、**アプリのアンインストールで失効する**。入れ直した場合はフォルダの再指定が必要。
- フォルダを差し替える／クリアするときは、古いURIの権限を `releasePersistableUriPermission` で解放する（端末ごとに保持できる永続URI数に上限があるため）。

## 9. 壁紙の暗さ

他人の目がある場所でも使いやすいよう、表示時にアプリ側で壁紙を暗くする。

### 9.1 決定事項

| 項目 | 決定 |
|---|---|
| 指定方法 | スライダー（連続値、1%単位） |
| 範囲 | 0%（元の明るさ）〜 **80%** |
| 単位 | 内側／外側で**別々**。内外でパネルが異なり見え方が変わるため |
| プロファイルとの関係 | **非依存**。10章のプロファイルを切り替えても暗さは変わらない |
| 初期値 | 0%（暗くしない） |

上限80%は実際に使って調整する前提の暫定値。変更は `WallpaperDimming.MAX_PERCENT` の1箇所だけで済む。

### 9.2 実装方式

画像を描いた上に黒を重ねる（`canvas.drawColor(Color.argb(alpha, 0, 0, 0))`）。

この方式を選んだ理由は、**デコード済みのビットマップを元のままキャッシュに残せる**こと。暗さを変えても再デコードは発生せず、描き直すだけで反映される。デコード時に暗さを焼き込む方式だと、キャッシュキーに暗さを含める必要があり、設定変更のたびに全画像の再デコードが走る。

描画コストは全画面の `drawColor` が1回増えるだけで、もともと背景塗りで1回呼んでいるため実質的な増加はない。

### 9.3 設定変更時の扱い

暗さの変更は**画像を選び直さず、描き直すだけ**にする。`WallpaperStore.isImageSourceKey()`（フォルダ・切替間隔）と `isAppearanceKey()`（暗さ）でキーを区別し、Engine 側で処理を分ける。暗さを調整しただけで表示中の画像が変わってしまうのを防ぐため。

設定画面のプレビューにも同じ暗さを重ね、実際の見え方を確認しながら調整できるようにする。スライダー操作中はプレビューのみ更新し、指を離した時点で保存する（保存のたびに壁紙が描き直されるため）。

### 9.4 システム標準のdim APIが使えない理由

`WallpaperManager.setWallpaperDimAmount()` は `@SystemApi` かつ `@hide` で、`SET_WALLPAPER_DIM_AMOUNT` 権限を要するため一般アプリからは呼べない。自前描画が唯一の手段。

### 9.5 設定画面のレイアウト

暗さを調整するにはプレビューとスライダーが同時に見えている必要がある。実機確認を踏まえて以下の構成にした。

**プレビューは高さ基準にする。** 当初は幅いっぱいにアスペクト比を掛けていたため、外側（縦長 1:2.17）では幅363dp × 2.17 = 高さ約790dpとなり画面をほぼ占有していた。高さを `PREVIEW_HEIGHT_DP`（初期値240dp）に固定し、幅をアスペクト比から決めることで、外側は幅約110dp・内側は約231dpに収まる。この定数を変えるだけで調整できる。

**画面幅に応じて2面の並べ方を変える。**

| 表示中の面 | レイアウト |
|---|---|
| 内側（幅が広い） | 内側・外側を**横並び**で同時に表示 |
| 外側（幅が狭い） | **タブ**で内側／外側を切り替え |

判定には `DeviceProfile.classify()` を設定画面のウィンドウ実寸に対して使う。分割画面などで幅が狭くなった場合も自動的にタブ側へ倒れる。

**面ごとの状態は画面側で保持する。** タブを切り替えるたびに SAF のフォルダ走査が走らないよう、両面の状態をまとめて読み込んで保持する。

**フォルダ設定は折りたたむ。** プレビューとスライダーの表示面積を優先する。ただし畳んだ状態でもフォルダ名・画像枚数・エラーは常に見えるようにし、設定できているかは一目で分かるようにする。

**情報は別画面にする。** 診断表示は頻繁に見るものではないためタブを占有させず、右上のボタンから開く別画面に置く。画面数が2つだけなので Navigation ライブラリは使わず、状態による切り替えと `BackHandler` で実装する。

## 10. プライベート／標準の切り替え

一人でいるときは任意の壁紙、他人の目があるときは無難な壁紙、という使い分けを行う。

### 10.1 決定事項

| 項目 | 決定 |
|---|---|
| 判定方法 | **マナーモードのみ** |
| 向き | マナーOFF（着信音あり）= **プライベート** / マナーON = **標準** |
| プロファイル | **2つ固定**（プライベート／標準） |
| フォールバック | 判定不能・想定外の値はすべて**標準**（安全側） |

`AudioManager.getRingerMode()` の3値を以下に対応させる。権限は不要。

| 定数 | 値 | プロファイル |
|---|---|---|
| `RINGER_MODE_NORMAL` | 2 | プライベート |
| `RINGER_MODE_VIBRATE` | 1 | 標準 |
| `RINGER_MODE_SILENT` | 0 | 標準 |

即時反映は `RINGER_MODE_CHANGED_ACTION` の動的登録で行う（権限不要）。この intent はシステムからのみ送られる protected intent なので、受信側に権限は要らず、`RECEIVER_NOT_EXPORTED` で登録する。

判定ロジックは `WallpaperProfile.kt` の `ProfileSelection` に集約してある。判定の向きを変えたくなったらこのファイルだけを見ればよい。情報画面の表示と壁紙の出し分けは**同じ関数を呼ぶ**ので、表示と実動作がずれることはない。

### 10.2 採用しなかった案

以下はいずれも検討の上で見送った。公共の場でマナーONを徹底する運用（必要ならTasker等で自動化）で対処する方針のため。

- **専用のクイック設定タイル**（`TileService`）— マナーモードより意図が明確で副作用もないが、運用で足りるため不要と判断
- **画面OFF／一定時間での自動復帰** — 切り忘れ対策になるが同上
- **ロック画面でのフェイルセーフ** — `KeyguardManager.isKeyguardLocked()`（権限不要）で「ロック画面表示中」を判定し、その間は強制的に標準プロファイルにできる。実装コストは小さいが同上

### 10.3 Androidの「モード」と連携できない理由（調査済み・再検討不要）

設定アプリの「モード」画面（サイレント モード／おやすみ時間／運転中など）と連動させる案を調査したが、**公開APIでは実現不可能**と確認した。同じ検討を繰り返さないために結論を記録する。

前提として、あの画面に並ぶものは**すべてDNDの仕組み**（`AutomaticZenRule`）で動いている。マナーモードとは別系統。

| 経路 | 結果 |
|---|---|
| `getAutomaticZenRules()` | `owned by the caller` = **自分が作ったルールのみ**。加えてDNDアクセス権が必須 |
| `getAutomaticZenRule(id)` | 同上。`doesn't own the matching rule` なら null |
| `getAutomaticZenRuleState(id)` | access がなければ常に `STATE_UNKNOWN` |
| `getCurrentInterruptionFilter()` | 権限不要だが**強さ(1〜4)のみでモード名を含まない** |
| `NotificationManager.getZenMode()` | `@hide` |
| `Settings.Global.ZEN_MODE` | `@hide`。しかも値が0〜3で**名前を含まない** |
| `NotificationListenerService` | `onInterruptionFilterChanged(int)` のみ。名前なし。重い権限が必要 |
| `SystemZenRules` / `ZenModeConfig` | `@hide` |

名前が取れない理由は2重になっている。(1) アクセス制御が所有者単位で、システム所有のモードは権限を取っても読めない。(2) そもそもモード名を運ぶ公開APIがなく、`@hide` APIを叩いても数値しか得られない。

**場所ベースの判定**（自宅Wi-Fi照合）も検討した。`ConnectivityManager` 経由で `ACCESS_NETWORK_STATE`（normal権限）のみでルーターIP・DNSサーバーIPが取得でき、位置情報権限なしで自宅ネットワークを識別できる。ただし要件が「場所」ではなく「他人の目があるか」だったため不採用。将来必要になった場合はこの経路が使える。

### 10.4 実装上の要点

**切り替えの契機**

`shouldAdvance()` の条件に「前回描画したプロファイルと違う」を追加した。`lastRenderedProfile` は `lastRenderedSide` と同じく永続化しているので、Engine が作り直されてもプロセスが死んでも取りこぼさない。

加えて Engine が `RINGER_MODE_CHANGED_ACTION` を購読しているため、**壁紙が見えている最中にマナーモードを切り替えても即座に反映される**。

**先読みは切り替え時に必ず捨てる（重要）**

先読み済みの画像は、選んだ時点のフォルダから取られている。プロファイルが切り替わった後にこれをそのまま昇格させると、**切り替え前のフォルダの画像が表示される**。プライベート用の画像が標準プロファイルで出てしまい、要件違反になる。

そのため `lastRenderedProfile` が現在のプロファイルと異なる場合は、`advance()` の前に `discardPrefetched()` で先読みを破棄する。

**キャッシュファイルは増えない**

表示中／先読みの画像は面ごとに1枚ずつのまま。プロファイルが変われば `advance()` で上書きされるので、プロファイル別に持つ必要がない。一覧ファイルだけは参照先フォルダが異なるため `list_<面>_<プロファイル>.txt` と組ごとに分けている。

**既存設定の移行**

プロファイル導入前のキー `folder_<面>` は、`WallpaperStore` の初期化時に `folder_<面>_standard` へ移す。移行先を標準にするのは、プライベート用は明示的に設定してもらうべきだから。何度呼んでも安全に書いてある。


## 11. 追加機能の実装経緯

当初の仕様（開閉での出し分け）を実装したのち、以下の3段階を追加した。いずれも実機で動作確認済み。

| 段階 | 内容 |
|---|---|
| 1 | 壁紙の暗さ（9章）＋設定画面のレイアウト刷新（9.5） |
| 2 | 情報画面の内容を充実させる（11.1） |
| 3 | プライベート／標準の切り替え（10章） |

段階2を段階3より先に置いたのは、**判定ロジックを壁紙に繋ぐ前に、情報画面で判定の挙動だけを実機確認できるようにするため**。表示と実動作は同じ `ProfileSelection` を呼ぶので、確認した挙動がそのまま本番の動作になる。

### 11.1 情報画面の内容

情報画面の枠は段階1で作成済み（9.5参照）。段階2で以下を追加した。表示は上から順に3つのカード。

**1. プライベート判定の状態**（動作確認用）

マナーモードの現在値（着信音あり／バイブ／サイレント／不明）と、適用されるプロファイルを表示する。`RINGER_MODE_CHANGED_ACTION` を購読しているので、**情報画面を開いたままマナーモードを切り替えると表示がその場で変わる**。段階3の判定ロジックを組む前に、実機で挙動を確かめられる。

判定そのものは `ProfileSelection`（10章）を呼ぶだけで、段階3ではこの同じ関数を壁紙の出し分けに使う。表示と実動作がずれる心配がない。

**2. 壁紙の描画領域**（壁紙素材を作るための値）

内側／外側それぞれの実測サイズ・アスペクト比・測定日時。未測定の面には「端末を開いて（閉じて）ホーム画面を表示してください」と操作方法を出す。

この値の取得には注意が要る。**両画面の解像度を同時に取得するAPIは存在しない**。

- `DisplayManager.getDisplays()` は「今有効な論理ディスプレイ」のみを返す。折りたたみ端末は内外で論理ディスプレイを共有するため両方は列挙されない
- `Display.getSupportedModes()` は「might include synthetic modes」とされ、合成モードが混ざる上に内外の判別ができない
- 設定画面のウィンドウサイズは壁紙サーフェスのサイズと一致しない（7.3参照）

したがって、**Engine が `onSurfaceChanged` で受け取った実寸を面ごとに保存**する方式を採った。描画のたびに呼ばれるため、**サイズが変わったときだけ書き込む**。端末を一度開いて一度閉じれば両方の値が揃う。

なおこの記録キーは `isImageSourceKey()` にも `isAppearanceKey()` にも該当しないため、Engine が自分の書き込みに反応して描き直すことはない（9.3参照）。

**3. 設定画面のウィンドウサイズ**（既存の診断カード）

`DeviceProfile` のレンジを実測値に合わせるために使う。

当初は2（壁紙サーフェス）とずれる想定で併記したが、**実機では両者が一致することが確認された**（7.3参照）。現状このカードは「いま表示中の面の判定結果をその場で見る」用途のみ。将来の機種でずれが生じたときに比較できるよう残してある。
